var nome = prompt("Digite seu nome: ");
var idade = Number(prompt("Digite sua idade: "));
    // retorno no terminal
   //console.log("Olá, " + nome + "!");
   // as crases são sensíveis, por isso o nome da variável deve ser exatamente igual e ele é a forma mais limpa de concatenar strings
alert(`Olá, ${nome}! Sua idade é ${idade} anos.`);
